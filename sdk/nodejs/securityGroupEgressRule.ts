// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * Manages egress rules for a security group using the batch API. This resource replaces all egress rules for the security group. This is an optional alternative to managing rules through the thalassa.SecurityGroup resource. Warning: Do not use both this resource and egressRule in thalassa.SecurityGroup for the same security group, as this will cause conflicts.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as thalassa from "@sandervb2/pulumi-thalassa";
 *
 * // Create a VPC for the security group
 * const exampleVpc = new thalassa.Vpc("exampleVpc", {
 *     description: "Example VPC for security group egress rules",
 *     region: "nl-01",
 *     cidrs: ["10.0.0.0/16"],
 * });
 * // Create a security group
 * const exampleSecurityGroup = new thalassa.SecurityGroup("exampleSecurityGroup", {
 *     description: "Example security group",
 *     vpcId: exampleVpc.id,
 *     allowSameGroupTraffic: false,
 * });
 * // Create egress rules for the security group
 * const exampleSecurityGroupEgressRule = new thalassa.SecurityGroupEgressRule("exampleSecurityGroupEgressRule", {
 *     securityGroupId: exampleSecurityGroup.id,
 *     rules: [
 *         {
 *             name: "allow-all-outbound",
 *             ipVersion: "ipv4",
 *             protocol: "all",
 *             priority: 100,
 *             remoteType: "address",
 *             remoteAddress: "0.0.0.0/0",
 *             policy: "allow",
 *         },
 *         {
 *             name: "allow-https-outbound",
 *             ipVersion: "ipv4",
 *             protocol: "tcp",
 *             priority: 101,
 *             remoteType: "address",
 *             remoteAddress: "0.0.0.0/0",
 *             portRangeMin: 443,
 *             portRangeMax: 443,
 *             policy: "allow",
 *         },
 *     ],
 * });
 * export const securityGroupId = exampleSecurityGroup.id;
 * ```
 */
export class SecurityGroupEgressRule extends pulumi.CustomResource {
    /**
     * Get an existing SecurityGroupEgressRule resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: SecurityGroupEgressRuleState, opts?: pulumi.CustomResourceOptions): SecurityGroupEgressRule {
        return new SecurityGroupEgressRule(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'thalassa:index/securityGroupEgressRule:SecurityGroupEgressRule';

    /**
     * Returns true if the given object is an instance of SecurityGroupEgressRule.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is SecurityGroupEgressRule {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === SecurityGroupEgressRule.__pulumiType;
    }

    /**
     * List of egress rules for the security group
     */
    declare public readonly rules: pulumi.Output<outputs.SecurityGroupEgressRuleRule[] | undefined>;
    /**
     * ID of the Security Group to manage egress rules for
     */
    declare public readonly securityGroupId: pulumi.Output<string>;

    /**
     * Create a SecurityGroupEgressRule resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: SecurityGroupEgressRuleArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: SecurityGroupEgressRuleArgs | SecurityGroupEgressRuleState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as SecurityGroupEgressRuleState | undefined;
            resourceInputs["rules"] = state?.rules;
            resourceInputs["securityGroupId"] = state?.securityGroupId;
        } else {
            const args = argsOrState as SecurityGroupEgressRuleArgs | undefined;
            if (args?.securityGroupId === undefined && !opts.urn) {
                throw new Error("Missing required property 'securityGroupId'");
            }
            resourceInputs["rules"] = args?.rules;
            resourceInputs["securityGroupId"] = args?.securityGroupId;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(SecurityGroupEgressRule.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering SecurityGroupEgressRule resources.
 */
export interface SecurityGroupEgressRuleState {
    /**
     * List of egress rules for the security group
     */
    rules?: pulumi.Input<pulumi.Input<inputs.SecurityGroupEgressRuleRule>[]>;
    /**
     * ID of the Security Group to manage egress rules for
     */
    securityGroupId?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a SecurityGroupEgressRule resource.
 */
export interface SecurityGroupEgressRuleArgs {
    /**
     * List of egress rules for the security group
     */
    rules?: pulumi.Input<pulumi.Input<inputs.SecurityGroupEgressRuleRule>[]>;
    /**
     * ID of the Security Group to manage egress rules for
     */
    securityGroupId: pulumi.Input<string>;
}
