// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * Get a Kubernetes cluster role by name or slug
 *
 * ## Important Notes
 *
 * - **Lookup Methods**: You can find cluster roles by either `name` or `slug`
 * - **Unique Results**: The lookup will return an error if multiple roles match the criteria
 * - **System Roles**: System roles are included in the results and can be identified by the `system` field
 * - **Permission Rules**: All permission rules are included in the results for inspection
 *
 * ## Common Use Cases
 *
 * ### Lookup by Name
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as thalassa from "@sandervb2/pulumi-thalassa";
 *
 * const developer = thalassa.getKubernetesClusterRole({
 *     name: "app-developer",
 * });
 * ```
 *
 * ### Lookup by Slug
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as thalassa from "@sandervb2/pulumi-thalassa";
 *
 * const monitoring = thalassa.getKubernetesClusterRole({
 *     slug: "monitoring-readonly",
 * });
 * ```
 *
 * ### Use in Role Binding
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as thalassa from "@sandervb2/pulumi-thalassa";
 *
 * const existingRole = thalassa.getKubernetesClusterRole({
 *     name: "app-developer",
 * });
 * // Create binding using the looked up role
 * const binding = new thalassa.KubernetesClusterRoleBinding("binding", {
 *     clusterRoleId: existingRole.then(existingRole => existingRole.id),
 *     userId: "user-123",
 * });
 * ```
 *
 * ### Inspect Role Permissions
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as thalassa from "@sandervb2/pulumi-thalassa";
 *
 * const role = thalassa.getKubernetesClusterRole({
 *     name: "app-developer",
 * });
 * export const rolePermissions = {
 *     role_name: role.then(role => role.name),
 *     rules_count: role.then(role => role.rules).length,
 *     resources: role.then(role => .map(rule => (rule.resources))),
 *     verbs: role.then(role => .map(rule => (rule.verbs))),
 * };
 * ```
 */
export function getKubernetesClusterRole(args?: GetKubernetesClusterRoleArgs, opts?: pulumi.InvokeOptions): Promise<GetKubernetesClusterRoleResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("thalassa:index/getKubernetesClusterRole:getKubernetesClusterRole", {
        "name": args.name,
        "organisationId": args.organisationId,
        "slug": args.slug,
    }, opts);
}

/**
 * A collection of arguments for invoking getKubernetesClusterRole.
 */
export interface GetKubernetesClusterRoleArgs {
    /**
     * The name of the Kubernetes cluster role to look up
     */
    name?: string;
    organisationId?: string;
    /**
     * The slug of the Kubernetes cluster role to look up
     */
    slug?: string;
}

/**
 * A collection of values returned by getKubernetesClusterRole.
 */
export interface GetKubernetesClusterRoleResult {
    /**
     * Annotations for the Kubernetes cluster role
     */
    readonly annotations: {[key: string]: string};
    /**
     * The timestamp when the Kubernetes cluster role was created
     */
    readonly createdAt: string;
    /**
     * A human-readable description of the Kubernetes cluster role
     */
    readonly description: string;
    /**
     * The unique identifier of the Kubernetes cluster role
     */
    readonly id: string;
    /**
     * Labels for the Kubernetes cluster role
     */
    readonly labels: {[key: string]: string};
    /**
     * The name of the Kubernetes cluster role to look up
     */
    readonly name?: string;
    readonly organisationId?: string;
    /**
     * List of permission rules for this Kubernetes cluster role
     */
    readonly rules: outputs.GetKubernetesClusterRoleRule[];
    /**
     * The slug of the Kubernetes cluster role to look up
     */
    readonly slug?: string;
    /**
     * Whether this is a system role
     */
    readonly system: boolean;
    /**
     * The timestamp when the Kubernetes cluster role was last updated
     */
    readonly updatedAt: string;
}
/**
 * Get a Kubernetes cluster role by name or slug
 *
 * ## Important Notes
 *
 * - **Lookup Methods**: You can find cluster roles by either `name` or `slug`
 * - **Unique Results**: The lookup will return an error if multiple roles match the criteria
 * - **System Roles**: System roles are included in the results and can be identified by the `system` field
 * - **Permission Rules**: All permission rules are included in the results for inspection
 *
 * ## Common Use Cases
 *
 * ### Lookup by Name
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as thalassa from "@sandervb2/pulumi-thalassa";
 *
 * const developer = thalassa.getKubernetesClusterRole({
 *     name: "app-developer",
 * });
 * ```
 *
 * ### Lookup by Slug
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as thalassa from "@sandervb2/pulumi-thalassa";
 *
 * const monitoring = thalassa.getKubernetesClusterRole({
 *     slug: "monitoring-readonly",
 * });
 * ```
 *
 * ### Use in Role Binding
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as thalassa from "@sandervb2/pulumi-thalassa";
 *
 * const existingRole = thalassa.getKubernetesClusterRole({
 *     name: "app-developer",
 * });
 * // Create binding using the looked up role
 * const binding = new thalassa.KubernetesClusterRoleBinding("binding", {
 *     clusterRoleId: existingRole.then(existingRole => existingRole.id),
 *     userId: "user-123",
 * });
 * ```
 *
 * ### Inspect Role Permissions
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as thalassa from "@sandervb2/pulumi-thalassa";
 *
 * const role = thalassa.getKubernetesClusterRole({
 *     name: "app-developer",
 * });
 * export const rolePermissions = {
 *     role_name: role.then(role => role.name),
 *     rules_count: role.then(role => role.rules).length,
 *     resources: role.then(role => .map(rule => (rule.resources))),
 *     verbs: role.then(role => .map(rule => (rule.verbs))),
 * };
 * ```
 */
export function getKubernetesClusterRoleOutput(args?: GetKubernetesClusterRoleOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetKubernetesClusterRoleResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("thalassa:index/getKubernetesClusterRole:getKubernetesClusterRole", {
        "name": args.name,
        "organisationId": args.organisationId,
        "slug": args.slug,
    }, opts);
}

/**
 * A collection of arguments for invoking getKubernetesClusterRole.
 */
export interface GetKubernetesClusterRoleOutputArgs {
    /**
     * The name of the Kubernetes cluster role to look up
     */
    name?: pulumi.Input<string>;
    organisationId?: pulumi.Input<string>;
    /**
     * The slug of the Kubernetes cluster role to look up
     */
    slug?: pulumi.Input<string>;
}
